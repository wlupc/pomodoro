# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

桌面番茄钟应用，纯前端单文件 HTML，双击 `pomodoro.html` 即可在浏览器中使用。

## 技术栈

- 纯 HTML/CSS/JS，无框架，无依赖，离线可用
- 声音通知使用 Web Audio API（无需音频文件）

## 开发

无需构建步骤，直接编辑 `pomodoro.html` 后在浏览器中刷新即可看到效果。
